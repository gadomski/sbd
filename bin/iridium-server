#!/usr/bin/env python

import SocketServer

from sbd.server import IridiumTcpHandler

HOST = "0.0.0.0"
PORT = 10800

server = SocketServer.TCPServer((HOST, PORT), IridiumTcpHandler)

server.serve_forever()
